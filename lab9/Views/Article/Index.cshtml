@model IEnumerable<lab9.Models.Article>

@{
    ViewData["Title"] = "Index";
}

<h1>Articles</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Image</th> @* Nowa kolumna na zdjęcie *@
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                Category
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @{
                    var imagePath = string.IsNullOrEmpty(item.ImageName) 
                        ? "/image/default.jpg" 
                        : "/upload/" + item.ImageName;
                }
                <img src="@imagePath" style="max-width: 50px; max-height: 50px;" alt="Product Image" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.Name) @* Zmiana: Category.Name *@
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
    <tfoot class="table-light" style="border-top: 2px solid #dee2e6;">
        <tr>
            <td></td> @* *@
            <td style="text-align: right; font-weight: bold;">Total Sum:</td>
            <td style="font-weight: bold;">
                @Model.Sum(i => i.Price).ToString("0.00")
            </td>
            <td></td> @*  *@
            <td></td> @* P *@
        </tr>
    </tfoot>
</table>
